version: "2"
sql:
  - engine: "sqlite"
    queries: "queries/"
    schema: "schema.sql"
    gen:
      go:
        package: "db"
        out: "."
        emit_db_tags: true
        emit_empty_slices: true
